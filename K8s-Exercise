On windows-open putty and login to the linux machine
make sure you are logged in with root. if not run, sudo su -
execute the following
mkdir .kube
cd .kube
touch config
vim config

download the config file from team and copy the content of config file to config file which you have created in .kube folder

execute the following
kubectl get nodes 
